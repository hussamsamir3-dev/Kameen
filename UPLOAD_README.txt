Kameen v2.3.4 — files with real edits: js/14_render.js, js/29_world.js, js/30_economy.js, js/33_device.js, css/game.css, manifest.js, index.html.
The other js/ files are included only because every script must carry the same version stamp (the boot check refuses mixed versions).
Upload all files here (overwrite). Footer must read v2.3.4.
