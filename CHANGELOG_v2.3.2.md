# Kameen v2.3.2
- The "تمييز تلقائي (مساعدة)" checkbox in the papers panel is **checked by default** and stays switchable per case (the global setting is left off on purpose, because when it is on the panel disables that checkbox).
- The conflict red pulse + calm two-note beep block from v2.3.1 had not actually been inserted (its anchor had already changed); it is in now.
